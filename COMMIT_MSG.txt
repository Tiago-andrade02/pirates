Premium mobile redesign: ProductCard add-to-cart, object-contain, carousel 80% peek

- ProductCard: now client component with "AGREGAR" button using cart context
- ProductCard: object-contain instead of object-cover for non-deformed images
- ProductCard: larger bold price, compact badges, consistent height
- ProductCard: e.preventDefault on add button so card navigation still works
- ProductCard: added feedback "Agregado" on add-to-cart
- images.ts: removed fs.existsSync dependency (was breaking client component)
- Carousel: 78% width on mobile (1 card + 22% peek of next)
- Catalog page: refined spacing, larger title, tighter product grid gaps
- Footer: h-9 touch targets on links, h-11 on social icons, tighter spacing
- All builds clean, no errors
