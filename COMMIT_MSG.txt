Fix navbar logo centering, white mobile menu, and perfumes page performance

- Navbar: use absolute centering on mobile, static on desktop for proper logo placement
- MobileMenu: white background with dark text/links, proper hover states
- images.ts: cache fs.existsSync results to avoid repeated filesystem calls per render
